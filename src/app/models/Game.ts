import { User } from "./User"

export class Game {
	_id: string;
	id: string;
	// gameTemplate: object;
	// createdOn: string;
	// startedOn: string;
	// endedOn: string;
	minPlayers: number;
	maxPlayers: number;
	// state: string;

	// createdBy: User;
	// players: User[];
}
